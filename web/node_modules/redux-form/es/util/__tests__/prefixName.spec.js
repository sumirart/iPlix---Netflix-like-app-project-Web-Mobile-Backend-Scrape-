"use strict";

var _prefixName = _interopRequireDefault(require("../prefixName"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('prefixName', function () {
  it('should concat sectionPrefix and name', function () {
    var context = {
      _reduxForm: {
        sectionPrefix: 'foo'
      }
    };
    expect((0, _prefixName.default)(context, 'bar')).toBe('foo.bar');
  });
  it('should ignore empty sectionPrefix', function () {
    var context = {
      _reduxForm: {
        sectionPrefix: undefined
      }
    };
    expect((0, _prefixName.default)(context, 'bar')).toBe('bar');
  });
});